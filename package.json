{
  "name": "@openmobile/android-device-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "Expose an Android device to coding agents and tooling: android CLI + adb core, MCP tools, localhost bridge, and an OpenCode feedback-loop plugin.",
  "bin": {
    "mcp-server": "src/mcp-server.ts"
  },
  "exports": {
    ".": "./src/index.ts",
    "./plugin": "./src/plugin/index.ts",
    "./bridge": "./src/bridge/main.ts"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.29.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2"
  }
}
