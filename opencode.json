{
  "$schema": "https://opencode.ai/config.json",
  "//": "Sample consumption config for the OpenMobile feedback-loop plugin.",
  "//2": "Points at the LOCAL plugin sample (see .opencode/plugins/openmobile.ts)",
  "//3": "so the plugin runs without publishing. After publish it can instead be",
  "//4": "loaded from the package export: node_modules/@openmobile/android-device-bridge/plugin",
  "plugin": [
    {
      "id": "openmobile-adaptive",
      "path": "./.opencode/plugins/openmobile.ts"
    }
  ]
}
